# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/
.Python

# Distribution / packaging
*.tar.gz
*.whl

# Unit test / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Environment — NEVER commit secrets
.env
.env.local
.env.*.local

# output/ is intentionally tracked — generated HTML is committed and served via Pages

# macOS
.DS_Store

# IDEs
.idea/
.vscode/
*.swp

# Claude Code — ignore user settings but track project slash commands
# (cannot use !negation on a fully-ignored dir; force-added instead)
.claude/
